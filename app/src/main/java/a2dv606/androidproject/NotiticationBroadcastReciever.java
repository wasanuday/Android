package a2dv606.androidproject;

import android.content.Intent;

import java.util.Date;

import a2dv606.androidproject.AppService;

/**
 * Created by Hussain on 3/31/2017.
 */

public class NotiticationBroadcastReciever {
           /*
        get extra from intent and start the service
         */
    //   boolean insertRecord = intent.getExtras().getBoolean("database");
    //   boolean isEnable = intent.getExtras().getBoolean("notification_enable");
/*
    String operation = intent.getExtras().getString("operation");
    if(operation.equals("database"))
    {  insertNewRecord();
        System.out.println("app broadcast for database fired"+new Date());
    }
    else if (operation.equals("notification")){
        Intent intent1 = new Intent(context,AppService.class);
        context.startService(intent1);
        System.out.println("app broadcast for notfication fired"+new Date());*/
}
